# OS / editor cruft
.DS_Store
*.swp

# TLS certificates / private keys
certs/

# C build artifacts (openarm-c)
openarm-c/*.o
openarm-c/openarm

# runtime artifacts
*.log
arm_calib.txt
openarm-c/arm_calib.txt

# scratch clone of upstream (if present)
upstream-openarm/

# leftover Python env/caches (old stable)
venv/
.venv/
__pycache__/
*.py[cod]

# external lib (reference)
rover-servo-lib/
